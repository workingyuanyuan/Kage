---
name: kage
description: '把內容排成暗色類比質感的網頁文件：單頁方案、長篇報告、書信、作品集、更新日誌、個股研報、簡報、產品落地頁。純黑基底、暖色漏光、膠片顆粒、襯線字主導，質感取自實體印刷品而非螢幕。觸發語包括「排版 / 做一份文件 / 一頁紙 / 執行摘要 / 白皮書 / 長文 / 作品集 / 更新日誌 / 版本紀錄 / 個股研報 / 投資備忘 / 簡報 / slides / 落地頁 / 官網 / 產品頁」，或「幫我做份提案 / 把這些整理成一份報告 / 做個作品集 / 這份看起來太醜了幫我排一下」。'
---

# 影 · kage

**影 · かげ** —— 光留下的痕跡。

一套設計語言貫穿八種文件與落地頁：純黑基底、錨在畫面外的暖色漏光、永遠最上層的膠片顆粒、襯線字主導的層級。材質取自電影影像 —— 漏光是底片走光與鏡身漏光，顆粒是膠片顆粒，中央沉暗是打光的結果。目標是實體印刷品的觸感，不是螢幕的銳利。

---

## 執行契約

動手前先鎖定：**類型、語言、語氣模式、頁型分級、長度目標、驗證指令**。能從對話推斷就推斷，只有在缺了會讓交付物長得不一樣時才問。

**提問預算（全域）**：先推斷。至多問一次，一則精簡訊息內最多兩個子問題，且僅在**兩個以上**維度真的無法推斷、或兩種類型真的都成立時才問。不要把待確認事項列成清單丟給使用者。

用最接近的既有模板與既有驗證路徑。除非當前需求非此不可，否則不新增模板、不新增共用 CSS 層、不新增依賴、不新增腳本旗標、不新增可選模式。

---

## Step 1 · 選類型與語言

| 使用者說 | 類型 | 模板 |
|---|---|---|
| 一頁紙 / 方案 / 執行摘要 | 單頁方案 | `one-pager.html` |
| 白皮書 / 長文 / 技術報告 / 年度總結 / 企劃提案 | 長篇報告 | `long-doc.html` |
| 正式書信 / 推薦信 / 辭職信 / memo | 書信 | `letter.html` |
| 作品集 / 案例研究 | 作品集 | `portfolio.html` |
| 更新日誌 / 版本紀錄 / release notes | 更新日誌 | `changelog.html` |
| 個股研報 / 估值分析 / 投資備忘 | 個股研報 | `equity-report.html` |
| 簡報 / 投影片 / deck | 簡報 | `slides.html` |
| 落地頁 / 官網 / 產品頁 | 落地頁 | `landing-page.html` |

**語言**：八種類型各有中英兩版，共 16 份模板。英文直接用 `-en` 檔（`one-pager-en.html`、`long-doc-en.html`、`letter-en.html`、`portfolio-en.html`、`changelog-en.html`、`equity-report-en.html`、`slides-en.html`、`landing-page-en.html`），不要拿繁中模板改。`-en` 模板的 `<html lang="en">` 已經是對的，不必動 —— 拉丁文的行高與行寬（`:lang(en)` 的 `line-height: 1.70` 與 `max-width: 60ch`）全都掛在這個屬性上，中文的 38em 行寬套在英文上會太寬。反過來也一樣：拿 `-en` 模板做中文文件時，`lang` 要改回 `zh-Hant`，否則正文會套到 60ch。

### 決策樹（問之前先跑這張）

| 訊號 | 類型 |
|---|---|
| 長度目標未知 | 先問長度，再分類 |
| 一屏內 + 投資人／主管／執行摘要 | one-pager |
| 一屏內 + 正式往來（業務、聘用、請辭） | letter |
| 大量視覺 + 專案陳列 | portfolio |
| 持續論證 + 視覺密度低 | long-doc |
| 演講流程 + 逐頁主張 | slides |
| 財務指標 + 論點 + 目標價或風險視角 | equity-report |
| 逐版本紀錄 + 發布事實 | changelog |
| 產品陳列 + 定價 + 截圖 + FAQ | landing-page |

真的兩格都成立才問一句。例：「兩頁執行摘要但要有指標卡」→ 問 one-pager 還是 equity-report。

---

## Step 2 · 來源與材料

文件依賴使用者草稿以外的事實或素材時才跑；使用者已經給齊的個人稿件跳過。

**來源檢查**在文件提到公司、產品、人物、發布日期、版本號、募資、指標、市場事實或技術規格時觸發。用一手來源，記下驅動文件的事實各自的來源與日期，來源衝突時問使用者，不要自己挑一個。細節見 `references/writing.md`〈來源先於文案〉—— 包含哪些「聽起來很新」的說法在查證前禁止寫出（最新、最近、新推出、版本號、發布日期、財務數字）。

**材料檢查**在主題是公司、產品、專案、場地或個人品牌時觸發：

| 需要 | 何時必要 | 可接受 |
|---|---|---|
| 品牌標誌 | 任何有品牌的文件 | 使用者檔案或官方 SVG/PNG |
| 產品圖片 | 實體產品／場地／物件 | 官方實拍、使用者提供，或標示缺口 |
| UI 截圖 | 應用／SaaS／網站／工具 | 當前截圖、官方產品圖，或使用者擷取 |

缺件給一張精簡的缺口表加一個問題，**絕不用替代品填坑** —— 不用無關意境圖、不重繪近似標誌、不編造數值。

圖片缺件在版面上有既定的呈現方式，不要自己想：把 `<img>` 換成 `.plate-wrap--gap` 佔位塊，規格見 `references/design.md`〈3.4 圖片框架〉的〈缺圖態〉。它維持與真圖相當的佔位高度 —— 這不是美觀考量，portfolio 的封面是負邊距交疊構圖，plate 一塌標題就會砸在圖說上。

材料檢查完輸出一次狀態區塊，這是單向透明顯示，不是提問：

```
材料狀態：
- 品牌標誌：OK  assets/client-logo.svg
- 產品截圖：缺　（已在版面標示缺口，未填替代圖）
- UI 截圖：不需要
```

只用 `OK`、`缺`、`不需要`。必要項目缺失且使用者沒回應，就帶缺口表問一次；其餘情況安靜繼續。

---

## Step 3 · 語氣模式與頁型分級

**語氣模式**（細節見 `references/writing.md`〈兩種語氣模式〉）：

- **標準模式**是預設。專業、直述、可被引用。
- **顯影模式**罕用，必須使用者明確指定才啟用。它把抽象宣示轉譯成可觀察的物理紀錄，不是把散文寫華麗 —— 氣氛由材質承擔，文字承擔結構。地板是「可以不用數字，但必須指得出東西」。

品質門檻與內容契約兩個模式完全相同。切模式不放寬任何一條。

**頁型分級**決定漏光強度與裝飾預算，已寫進模板，不要自己改：

| 級別 | 用於 | 模板掛法 |
|---|---|---|
| A 敘事型 | portfolio · slides · landing-page | `<body>` 不掛類別（預設值） |
| B 結構型 | 局部區塊（列表、儀表板區） | 該區塊掛 `.tier-structural` |
| C 閱讀型 | one-pager · long-doc · letter · changelog · equity-report | `<body class="tier-reading">` |

slides 逐頁掛 `data-tier="a|b"`：封面與引用頁 A，內容頁 B。

**C 級的正文欄必須置中，且不超過 `--measure`。** 這條被違反時，正文會坐進邊緣漸亮區。

---

## Step 4 · 讀多少規格

取涵蓋任務的**最低**一層，不要全部打開。

| 層 | 何時 | 讀 |
|---|---|---|
| 純內容 | 改文字、換要點、翻譯既有文件，CSS 不動 | `references/writing.md` 對應類型的策略與門檻 |
| 版面微調 | 調間距、搬區塊、改字級，CSS 會動 | 上述 + `references/design.md` |
| 新文件 | 從零或從原始素材做 | `design.md` + `writing.md` + `anti-patterns.md` + 模板 + `references/schemas/<type>.json` |
| 背景／材質 | 動到漏光、顆粒、表面、色彩 | `references/material.md` |
| 來源／材料 | 主題是公司、產品、市場、發布、募資 | `writing.md` 來源規則 + 使用者素材 |
| 審查草稿 | 交付前檢查 AI 生成的稿子 | `references/anti-patterns.md` |
| 圖解 | 要不要畫圖、畫哪種、圖說怎麼寫 | `writing.md`〈圖解判斷〉 |

---

## Step 5 · 填製

- **複製模板到工作目錄**，不要從零手寫 HTML。
- **沒有自動套版腳本，填製由你逐格進行。** 模板的 `{{...}}` 是寫給你看的**指示文字**（例如 `{{論點一：說明公司競爭壁壘…}}`），不是機器鍵，JSON 的欄位名與它們沒有對應關係。不要去找 render/fill 指令，那不存在，也不會有。
- 契約先行：內容先寫成符合 `references/schemas/<type>.json` 的 JSON，用 `node scripts/kage.mjs content <type> <稿件.json>` 驗過語意形狀與長度，再依模板指示逐格填入。長度上限是版面約束，不是建議。
- 填之前先讀該類型在 `writing.md`〈各類型的品質門檻〉的那幾條。**填滿每個欄位是必要條件，不是充分條件** —— 小標是不是完整陳述句、證據有沒有帶數字、有沒有寫出取捨，跟填滿一樣重要。
- **填內容時 CSS 不動。** 真的要改樣式，改動必須同步 `references/design.md` 與其餘十五份模板（中英各八），絕不只改一份；`node scripts/kage.mjs sync --write` 把 BASE 區塊傳播到 `assets/templates/` 的模板庫。這是維護模板庫的工具，不會動你工作目錄裡填好的成品。

### 不要生成

- 不要編造指標、財務數據或統計；缺口寫成 `[需要資料：說明]`，不要留空也不要猜。整串必須是**單一文字節點**，拆進 `<b>` 之類的子元素 `placeholders` 就撿不到，缺口會漏報。
- 不要用意境圖描述當圖片佔位（「一支多元團隊在現代辦公室協作」）。圖片缺件用 `.plate-wrap--gap`，見 Step 2。
- 不要為了填滿版位而灌水。三個真實案例的作品集不需要湊成五個。
- 不要在 24px 以下的文字用橘棕色（對比只有 3.98:1，漏光區 2.85:1）。
- 不要用 emoji 或圖示當項目符號。

---

## Step 6 · 驗證

```bash
node scripts/kage.mjs check                             # BASE、token、lint、內容契約
node scripts/kage.mjs content <type> <稿件.json>        # 語意形狀與長度上限
node scripts/kage.mjs placeholders <完成的檔案>          # 殘留的 {{}} 與資料缺口
node scripts/kage.mjs shot <完成的檔案> --widths 1280,375  # headless Chrome 截圖
node scripts/kage.mjs shot <完成的檔案> --bg-only        # 只有背景層的截圖
node scripts/kage.mjs bg <背景截圖.png…>                 # 背景層八條像素驗收
```

模板本身刻意保留 `{{}}`，`placeholders` 只跑在完成的文件上，不要跑整個模板庫。它同時列出 `[需要資料：…]` 缺口 —— 缺口不算失敗，但那份清單就是收尾訊息第 3 條要回報的東西。

**螢幕驗證是硬步驟，不是加購項。** 交付前要在 **1280px 與 375px** 兩個寬度看過畫面：找孤字、稀疏區塊、單行表面換行、水平捲軸。不要等使用者問「手機上會不會壞」。

**背景是統計問題，不是目視問題。** 顆粒強度、明度分布、色相在原始碼裡看不出來，眼睛也不可靠 —— 用 `bg` 量。

**但一定要餵背景截圖（`shot --bg-only`），不要餵含內容的截圖。** 含內容的畫面裡，中央的紙張表面與米白正文會直接主宰明度統計：實測一份正確的信件，中央明度 128、正文欄 p99 226，八條裡五條報紅 —— 量到的全是前景，背景層其實好好的。`--bg-only` 會暫時隱藏 `.bg-fixed` 以外的所有元素再截圖。

`shot` 用系統上的 Chrome 或 Edge，兩者都找不到時設 `KAGE_CHROME` 指向執行檔。真的沒有瀏覽器可用時：跑完機械檢查，然後照「完成的定義」第 4 條誠實陳述視覺未確認 —— 那不是略過驗證的許可，是禁止你在沒看過畫面時宣稱看過。

### 完成的定義

收尾訊息必須包含：

1. 每份交付物的路徑。
2. 跑了哪些檢查、結果各是什麼。
3. 每一個還沒填的 `[需要資料：…]` 缺口，逐條列出。**有未回報的缺口就不算完成。**
4. 視覺結論，按表面誠實陳述：1280/375 截圖看過的結果；沒能看到畫面時就寫「已驗證建置，視覺未確認」，不要寫「完成」。

**機械檢查全綠不等於填好了。** `placeholders` 只證明「沒有 `{{}}` 殘留」—— 把佔位符換成任何字串它都會過，包括「詳見內文」「（待補）」「產品名稱」這種等於沒寫的字。`content` 同理：它驗欄位形狀與長度上限，不驗那段話有沒有內容。**唯一能判斷填得對不對的是你自己**，依據是 `writing.md`〈各類型的品質門檻〉那幾條：小標是不是完整陳述句、證據有沒有帶數字、有沒有寫出取捨。收尾前逐條對過那些門檻再說完成，不要拿工具的綠燈當交差憑據。

---

## 回饋協定

使用者給**模糊的視覺回饋**（「怪怪的」「太擠了」「不夠精緻」）時不要猜。回問時指名元素與它當前的值，並給兩個仍在規格內的選項：

> 「X 目前是 Y。你要 (a) ⋯⋯ 還是 (b) ⋯⋯？」

不要說「我來調一下間距」而不指名確切的屬性與新值。

**兩輪未過就升級。** 同一個元素調兩輪還沒過，停止微調數值：做一張對照圖，把當前狀態與 2–3 個標了 A/B/C 的變體用同一份內容並排。字體、強調色、標誌這類沒有客觀判準的選擇，直接跳過微調，一開始就給樣張：至多 5 個候選，每個是同樣標題加段落的半頁區塊。一輪「挑一個」會收斂，五輪「再試試」不會。挑定之後一次套用到所有地方。

---

## 什麼時候不要用這個 skill

- **要可編輯的原生檔案**（.docx、.pptx、.xlsx）—— 這裡只產出 HTML。
- **要列印或 PDF 交付** —— 本專案是純螢幕設計語言，深色底不適合列印，`@media print` 只是降級不是交付路徑。
- **淺色、明亮、企業感的視覺需求** —— 那不是把 token 換掉就能達成的，整套設計語言（漏光、顆粒、反向暗角）都建立在暗色上。
- **純資料儀表板或即時應用介面** —— 這是文件排版，不是應用框架。
